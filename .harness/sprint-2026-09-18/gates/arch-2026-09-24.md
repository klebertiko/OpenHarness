# Architecture Gate — Sprint 2026-09-18 — 2026-09-24

Owner: Claude, inline (ARCH is collapsible into the orchestrating session per ORCHESTRATION.md).
Entry: QA exit state "Approved for Architecture Review" for CHAT-TOOLS-CONTRACT, CHAT-TOOLS-BE,
DIRECT-HISTORY, AUTOMATE-CHECKPOINT (`gates/qa-2026-09-23.md`).

## Checklist

- **Code follows established patterns** — PASS. All new/touched backend routers
  (`routers/chat_tools.py`) use the existing `APIRouter` + `Depends(get_db)` shape identical to
  the rest of `routers/*.py` (spot-checked 9 router files; the two pre-existing exceptions,
  `bundles.py`/`repos.py`, predate this sprint and aren't touched by it). `automations/scheduler.py`
  (AUTOMATE-CHECKPOINT) follows the existing class/async-function shape already used there.
- **No ADR violations** — PASS. Read `docs/adr/0004-chat-tools-broker.md` decision-by-decision
  against the actual code: (1) broker-only — confirmed `routers/chat_tools.py` exposes only
  `GET /capabilities`, `GET /discover`, `POST /read`, no standalone exec route; (2) exec runs only
  as an SSE event inside `/execute/direct`, gated through the existing `RunControl` mechanism —
  confirmed, no second execution path exists; (3)–(6) match `sandbox/` module layout and the
  contract's Skill≠Tool menu semantics already verified by QA. Status field updated from
  "Proposto" to "Aceito" on this ADR now that it's implemented, tested and reviewed.
  ADR 0001 (desktop packaging) and 0003 (ohm yaml) aren't touched by these four stories — no
  conflict surface.
- **Schema changes have migrations** — N/A, correctly. This project has no migration framework
  (`database.py` uses `Base.metadata.create_all`, additive-only). DIRECT-HISTORY's `source` field
  is stored inside `ExecutionLog.result_json` (existing `Text` JSON blob), not a new column — no
  schema change occurred at all, consistent with the project's own established pattern for this
  kind of addition.
- **Performance reviewed** — PASS, lightweight desktop-app scale. `GET /execute/logs` is one
  query (`select(...).order_by(started_at.desc()).limit(50)`), no N+1. No unindexed-sort concern
  at this data volume (local SQLite, capped result set). Not re-auditing CHAT-TOOLS-BE's sandbox
  exec path here — QA's 2026-09-23 report already verified the process-tree/timeout code paths
  directly with reproduced tests.
- **PR created** — see below.
- **Evidence package for HITL** — this file + `gates/qa-2026-09-23.md` + the four story ledgers.

## Route

PASS → all four stories move to "Ready for Security Review" pending the PR link.

No pattern violations, ADR conflicts, or missing-migration issues found. CHAT-TOOLS-FE is not
included in this pass — it re-enters QA once its mutation gate closes (in progress, separate
STATUS line).
