# QA Gate — Sprint 2026-09-18 — 2026-09-23

Fresh-context QA, independent of both implementers. Environment check first: `git status` /
`pwd` ran normally (see below) — the 2026-09-22 bounce reason ("ambiente recusa criar processo")
does **not** reproduce today. This is a real audit, not a repeat of that bounce.

```
$ cd D:\Development\src\OpenHarness && git status
On branch feature/openharness-06-tauri-installer ... (ran fine)
```

## Cross-cutting finding — nothing in this sprint is committed

```
$ git log -1 --format="%H %ci"
3689da0af1407acb090eb5418243f52121c0429a 2026-09-09 23:38:45 -0300
$ git status --porcelain .harness/sprint-2026-09-18/story-CHAT-TOOLS-CONTRACT/ docs/adr/0004-chat-tools-broker.md
?? .harness/sprint-2026-09-18/story-CHAT-TOOLS-CONTRACT/
?? docs/adr/0004-chat-tools-broker.md
$ git status --porcelain | grep -E "sandbox|chat_tools"
?? backend/routers/chat_tools.py
?? backend/sandbox/
?? backend/tests/test_chat_tools_*.py / test_sandbox_*.py
```

The last commit on this branch predates the sprint by 9 days. Every file touched since
2026-09-18 — `contract.md`, `threat-model.md`, ADR 0004, `backend/sandbox/**`,
`backend/routers/chat_tools.py`, all `frontend/src/components/agent-run/**` — is uncommitted
working-tree state. `git worktree add` at HEAD confirmed this concretely: the checked-out
worktree had **none** of the sprint's code, because worktrees only materialize committed refs.
I had to manually copy (`robocopy`) the working tree into an isolated worktree to run isolated
E2E at all (see below) — the same workaround the ledgers show Codex using ("HEAD 3689da0 +
snapshot backend atual").

This blocks Gate 3 (ARCH: "PR created") for **every** story, regardless of what each story's own
DoD says. I bounce only the three stories whose own DoD explicitly lists "commitados" (below);
the other four pass QA but will bounce again at ARCH the moment PR creation is attempted, unless
commits land first. Recommend a commit pass across both territories before ARCH starts.

## Suites (run by me, this session)

- Backend full: `cd backend && .venv/Scripts/python.exe -m pytest -q` → **608 passed, 34 skipped
  in 51.49s** (ledger claimed 607/34 — 1 more passed now, not a regression).
- Frontend full: `cd frontend && npm run test -- --run` → **372 passed (68 files)** (ledger
  claimed 369 — consistent growth, not a regression).
- Frontend typecheck: `npx tsc --noEmit` → clean, no output.
- Backend E2E (28, normally isolated-only): reproduced independently by copying the working tree
  into a fresh worktree (`D:\Development\.worktrees\openharness-qa-20260923`, `.git` confirmed a
  **file** i.e. real linked worktree) and running `OH_ISOLATED_E2E=1 pytest tests/e2e/` →
  **28 passed in 83.04s**. Exact match to both CHAT-TOOLS-BE and DIRECT-HISTORY ledger claims.
- Mutation spot-check (backend): the worktrees that produced the numbers still had live
  `.mutmut-cache` SQLite files for two modules. Queried directly:
  - `openharness-mutation-final-20260922/process/.mutmut-cache` → `31 ok_killed / 3 bad_survived
    / 34 total = 91.18%` — **exact match** to CHAT-TOOLS-BE ledger's `process_tree.py` row.
  - `openharness-mutation-final-20260922/schemas/.mutmut-cache` → `88 ok_killed / 16 bad_survived
    / 104 total = 84.62%` — **exact match** to CHAT-TOOLS-BE ledger's `schemas.py` row.
  - `openharness-sprint-20260918-7fd22a38/backend/.mutmut-cache` → `45 ok_killed / 8 bad_survived
    / 53 total = 84.91%` — **exact match** to DIRECT-HISTORY ledger's claim.
  These three independent reproductions materially raise confidence in the rest of the
  (unspot-checked) mutation table, which was self-reported but consistent in method.
- Mutation/E2E (frontend, CHAT-TOOLS-FE + CONTEXT-GAUNTLET-R2): **could not be reproduced.**
  `D:\Development\.worktrees\openharness-fe-integration-20260918` (the worktree the ledgers cite
  for Stryker `mutation.json` and the Playwright run) no longer exists anywhere on disk —
  confirmed via `git worktree list` and a full-disk `find`. `frontend/package.json` has no
  `playwright` or `stryker` entry at all (only a transitive, unrelated mention of
  `@playwright/test` deep in `package-lock.json`), and no `playwright.config.*` or spec file
  exists in the checkout. STATUS.md's own last line (2026-09-23 16:57) says this worktree was
  about to be removed as "obsolete" — it was, before QA could verify it. This is real evidence
  loss, not a QA process failure.

## Per-story verdicts

### CHAT-TOOLS-CONTRACT — BOUNCE

- AC#1,2,4,5,6 — PASS. `contract.md` has JSON examples for all 4 endpoint groups (§2.1–2.4),
  root/symlink/timeout/byte-limit/approval rules per operation (§2, §4), the HTTP tool loop and
  CLI-adapter treatment with SEC-1/5 preserved (§3), and §1's table distinguishes
  Command/Skill/Tool unambiguously ("insere... rótulo 'inserir'" vs "abre um run... pede
  aprovação"). ADR 0004 exists at `docs/adr/0004-chat-tools-broker.md`, records the broker
  decision, alternatives rejected, and links both consumer stories.
- AC#3 — PASS (content). `threat-model.md` §2 STRIDE table: every one of the 15 threats has a
  named state (Fechado/Mitigar/Aceito) with a concrete mechanism, never "TBD". §3 has 2 attack
  trees, §4 ranks 8 mitigations by cost, §5 names 5 accepted risks as decisions.
- DoD "SEC fresh-context aprovou" — open, but not a Gate-2 blocker: GATES.md sequences SEC after
  ARCH (Gate 4 after Gate 3); no story in this sprint has reached SEC yet (zero `SEC-PASS` lines
  anywhere in STATUS.md). Flagging for Gate 4, not bouncing on it here.
- DoD "commitados" — **FAIL**. `contract.md`, `threat-model.md`, `features/*.feature`, and
  `docs/adr/0004-chat-tools-broker.md` are all untracked (`??`). See cross-cutting finding above.
- Route: **@claude** — `git add` + commit the story folder and the ADR (own territory per
  sprint.md's file-ownership table). Mechanical; re-signal "Ready for QA" once done.

### CHAT-TOOLS-BE — BOUNCE

- AC#1 (discover) — PASS: `test_chat_tools_discovery*.py` in the targeted run below (part of 171
  passed).
- AC#2 (read, 403 traversal/symlink) — PASS. Code: `sandbox/paths.py:13-29` resolves then checks
  `resolved.is_relative_to(base)`; `sandbox/read.py:20-22` raises `ReadFailure(...,403)` for
  denied/secret paths. Tests: `test_sandbox_paths.py`, `test_sandbox_read_contract.py`,
  `test_chat_tools_read_bdd.py`.
- AC#3/4 (exec, timeout/bytes, gate/409 not 428 per contract v1.1) — PASS. Code:
  `sandbox/approval.py:16-32` (`begin`/`decide`, `ApprovalConflict('call_id_mismatch')`).
  Tests: `test_sandbox_exec.py`, `test_chat_tools_control.py`.
- AC#5 (HTTP tool loop) — PASS: `test_chat_tool_loop.py` (loop/budget/fallback, part of targeted
  run).
- AC#6 (CLI flags unchanged) — not independently re-run by me this session (pre-existing
  `test_cli_claude_flags.py` covered by the full backend suite, 608 green).
- AC#7 (secret denylist) — PASS. Code: `sandbox/secrets.py:8-23` (denylist patterns,
  `.ssh/.aws/secrets` path segments). Test: `test_sandbox_secrets.py`.
- AC#8 (workspace propagation) — not independently spot-checked beyond the green full suite.
- Verification commands (this session):
  `pytest -q tests/test_sandbox_paths.py tests/test_sandbox_secrets.py tests/test_sandbox_exec.py
  tests/test_sandbox_read_contract.py tests/test_chat_tools_control.py
  tests/test_chat_tools_discovery.py tests/test_chat_tools_discovery_bdd.py
  tests/test_chat_tools_presets.py tests/test_chat_tools_read_bdd.py
  tests/test_chat_tools_cli_bdd.py tests/test_chat_tool_loop.py
  tests/test_execution_budget_enforcement.py` → **171 passed, 6 skipped in 17.98s** (skips are
  symlink-privilege-only, matches ledger).
  Isolated E2E (all 28, reproduced by me): **28 passed in 83.04s**.
  Mutation: 2/11 module rows independently reproduced exact (see Suites above); rest self-reported
  but consistent.
- DoD "Código + testes commitados" — **FAIL**. `backend/routers/chat_tools.py`, `backend/sandbox/`
  (11 files), and all `test_chat_tools_*`/`test_sandbox_*` test files are untracked.
- Route: **@codex** — commit `backend/**` for this story (own territory). Everything else checks
  out; this is the only gap.

### CHAT-TOOLS-FE — BOUNCE

- AC#1,2,5 — PASS. `chatCommands.test.ts` (10 tests) verified directly, including
  `it("A discovered prompt command without argv inserts its file instead of crashing")` at
  `chatCommands.test.ts:55` against `chatCommands.ts:115` (`else if (!item.argv && preset.read)` —
  routes an argv-less discovered item to an insertable draft instead of a broken exec preset).
  This independently confirms the "markdown command sem argv" fix the 2026-09-23 handoff claimed
  but that appears in no ledger — **confirmed by me via code + a passing, purpose-named test**,
  not trusted blindly.
- AC#3,4 — PASS via full suite + code read. `ToolCard.tsx:37-47` claims the read-disclosure
  sentence via a lazy `useState` initializer plus a zustand session store
  (`readDisclosure.ts`), so a remount after the store already recorded the provider renders
  `alreadyDisclosed=true` and stays silent — this independently confirms the handoff's second
  claimed fix ("disclosure remoto reaparecendo após rerender"). `runReducer.test.ts:207-214`
  (`"preserves capabilities notices emitted before run_start"`) independently confirms the third
  ("notice de capabilities chegando antes de run_start") — asserts `capabilities`/`notices`
  survive a later `run_start` action. All three ran green as part of the full 372-test suite.
- AC#6 — PASS. `SOUL.md:27-33` distinguishes HTTP tool-capable connections ("she can...") from
  CLI connections ("she cannot ask for tools"); no "no hands" language remains.
- Regression — PASS: full `npm run test -- --run` **372 passed (68 files)**; `tsc --noEmit` clean
  (both run by me this session).
- Mutation (Stryker, claimed 79.27%) and E2E (Playwright, claimed 3 passed + OHM roundtrip) —
  **UNVERIFIED**. Evidence destroyed: the worktree is gone, no report file exists anywhere on
  disk, and the tooling itself isn't in the checked-out `package.json`. Per sprint.md's own rule
  ("Stories sem as quatro camadas não sai de QA: QA-BOUNCE missing:<camada>"), this alone is
  bounce material.
- DoD "Integração manual contra BE real ... com screenshot no ledger" — **FAIL**. Ledger has text
  notes only (e.g. "03:02 | VISUAL | ..."); no image file exists anywhere under
  `.harness/sprint-2026-09-18/` (searched `*.png`/`*screenshot*`, zero hits).
- DoD "Código + testes commitados" — **FAIL**, same as above (all of `frontend/src/components/
  agent-run/**`, `chatCommands.ts`, `readDisclosure.ts` etc. are modified/untracked, uncommitted).
- Route: **@claude (FE)** — commit the code; re-install Playwright + Stryker as real
  devDependencies, rerun both in a **new** isolated worktree, and this time leave the report
  artifact in place (or copy it into the story folder) until QA has verified it — don't delete the
  evidence worktree before the gate that needs it runs. Attach an actual screenshot file for the
  manual-integration DoD item, not prose.

### DIRECT-HISTORY — PASS

- AC#1/2 — PASS: original RED test intact and green.
  `pytest tests/test_execution_provider_resolution.py tests/test_execution_logs_api.py
  tests/test_direct_history_bdd.py` → **26 passed in 4.23s** (run by me).
- AC#3 — PASS: `source: direct|harness` distinguishing field, covered by the same run (BDD +
  logs API tests).
- AC#4 — PASS: failed-provider persists `status: "failed"`, covered by the same run
  (`test_execution_provider_resolution.py` includes the failure-classification cases).
- E2E — PASS, independently reproduced: isolated-worktree run (28 total backend E2E, of which
  `test_direct_history_e2e.py`'s scenarios are a subset) → **28 passed in 83.04s**, by me.
- Mutation — PASS, independently reproduced: `openharness-sprint-20260918-7fd22a38/backend/
  .mutmut-cache` queried directly via sqlite → `45 killed / 8 survived / 53 total = 84.91%`,
  **exact match** to the ledger's claim, above the 70% target.
- Full backend regression green (608/34, run by me).
- DoD: no explicit "commitados" line in this story's own DoD (unlike CONTRACT/BE/FE) — not
  bounced on that basis, but the same cross-cutting commit gap applies and will resurface at
  ARCH's "PR created" check. Everything this story's DoD actually asks for is verified.
- No route needed. **Approved for Architecture Review.**

### AUTOMATE-CHECKPOINT — PASS

- AC#1/3/5 — PASS. `story-AUTOMATE-GAUNTLET/ledger.md:365-395` (checkpoint técnico Codex) has a
  per-piece table (1/2/4/6) with verdict, files touched, and named tests for each — all
  `parcial`, none inflated to `pronta`/WINNER. `story-AUTOMATE-REAL/ledger.md` exists with the
  same AC-by-AC reconciliation. Timezone/dedup/budget/cwd each get an explicit
  covered-vs-not-covered line (`ledger.md:378-383`), no invented coverage.
- AC#2 — PASS, reproduced by me: `pytest tests/test_scheduler_real_execute.py tests/test_scheduler.py
  tests/test_automation_validation.py tests/test_cowork_automations_api.py` → **58 passed in
  2.24s**, exact match to the ledger's claim. The always-mock mutation ("2 failed, 1 passed")
  demonstrates the test actually fails if real/mock selection regresses — not just present-and-
  green.
- AC#4 — vacuously satisfied: no piece was declared `pronta`, so the "pieces marked pronta get a
  fresh-context critic" requirement has nothing to trigger on. Ledger states this explicitly
  rather than skipping silently.
- DoD: ledgers updated ✓, suite green with real count ✓ (verified above), critic requirement N/A
  ✓. PO acceptance is a Sprint Review / Gate 5 activity, not a QA blocker.
- No route needed. **Approved for Architecture Review.**

### CONTEXT-GAUNTLET-R2 — BOUNCE

- AC#1,2,3 — PASS. `rollup.test.ts` (6 tests) and `Transcript.test.tsx` (11 tests) run directly
  by me, both green; part of the full 372-test suite. `rollupTotals()` returns
  `{elapsedMs, tokens, source}`, `source` flips `measured`/`estimated` on `harness_done`, and
  `rollupElapsedMs`'s existing null-not-zero contract is preserved (explicit regression test).
- AC#4 — **FAIL**. Story's own ledger says "AC#4 permanece reservado ao crítico fresh-context do
  gate QA/ARCH" (i.e., deferred to exactly this review). I checked
  `sprint-2026-09-15/story-CONNECTION-CONTEXT-GAUNTLET/ledger.md` for a round-2 critic entry —
  only `p4-critic-r1` (round 1, the one that found the original gap) exists; no `p4-critic-r2` or
  equivalent verdict is recorded anywhere. The required critic never ran.
- Mutation (claimed Stryker 16/16 on `rollup.ts`) — **UNVERIFIED**, same root cause as
  CHAT-TOOLS-FE: the story's own ledger ties this score to
  `openharness-fe-integration-20260918/frontend/reports/mutation.json`, which no longer exists.
  Stryker isn't in the checkout's `package.json` either.
- Route: **@claude (FE)** for the mutation re-run (same isolated-worktree fix as CHAT-TOOLS-FE,
  can be done together since both measure the same worktree). For AC#4, this is a
  gauntlet-loop-style fresh-context critic against the DeepSeek Harness Token Meter bar — route to
  whoever dispatches that critic (the story names it as a Claude-side responsibility); it must
  land in `sprint-2026-09-15/story-CONNECTION-CONTEXT-GAUNTLET/ledger.md`, not just this story's.

### DOSSIER-CRED-LENGTH — PASS

- AC#1 — PASS: `Dossier.test.tsx` — "does not invent a key length for a credential reference
  restored from backend" — run directly by me (5 tests in the file, all green); part of the full
  372-test suite.
- AC#2 — PASS. Root cause confirmed by direct code read, matches the ledger's claim: before the
  fix, `CredentialSeal.tsx` treated `length: 0` (a backend-restored reference with no metadata) as
  a real measurement, rendering "0 characters" and a negative-width bar. Current code:
  `CredentialSeal.tsx:145` (`s.length > 0 ? Math.max(0, s.length - s.prefix.length - 4) : null`)
  and `:152` (`s.length > 0 ? \`${s.length} characters\` : "not reported by the vault"`) — `null`
  now flows to `Redaction`, which renders "Credential value withheld; length unavailable"
  (`CredentialSeal.tsx:39-43`) instead of fabricating a number. The component was the bug, not the
  test, as the ledger states; the test's assertion is unchanged.
- DoD "Vitest 317/317" is a stale count from story-authoring time (suite has since grown via other
  stories); what matters — 0 failures — holds: full suite is 372/372 passed, `tsc --noEmit` clean.
- No route needed. **Approved for Architecture Review.**

## Decision

`QA-PASS: DIRECT-HISTORY, AUTOMATE-CHECKPOINT, DOSSIER-CRED-LENGTH`

`QA-BOUNCE: CHAT-TOOLS-CONTRACT (commits missing, @claude), CHAT-TOOLS-BE (commits missing,
@codex), CHAT-TOOLS-FE (commits missing + Stryker/Playwright evidence destroyed and tooling absent
from checkout + no screenshot for manual-integration DoD item, @claude), CONTEXT-GAUNTLET-R2 (AC#4
critic never ran + Stryker evidence destroyed, @claude)`

Cross-cutting, not a per-story bounce reason but flagged for @SM/@ARCH: nothing in this sprint is
committed to git (last commit predates the sprint by 9 days). The 3 PASS stories will still hit
Gate 3's "PR created" checklist item and bounce there unless commits land first.
